---

services:
  app:
    image: ghcr.io/mf450s/lubelogger_mcp:latest
    # environment:
    #  - LUBELOG_INSTANCE=
    #  - LUBELOG_USER=
    #  - LUBELOG_PASS=
    restart: unless-stopped
    # expose port and/or use serving via traefik
    ports:
      - 40150:8080